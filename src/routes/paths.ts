import { paramCase } from '@/utils/change_case';

//----------------------------------------------------------------

const ROOTS = {
	AUTH: "/auth",
	ADMIN: "/admin",
	DASHBOARD: "/dashboard",
	HOME: "/",
	DEV: "developer"
}

//-----------------------------------------------------------------

export const paths = {
	developer: {
		root: ROOTS.DEV,
	}
}